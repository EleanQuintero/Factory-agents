{
  "name": "zenith-factory-edge-gateway",
  "version": "0.1.0",
  "description": "Cloudflare Worker Edge Gateway for Zenith Factory Mastra VMs",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "0.4.0",
    "@supabase/supabase-js": "2.47.0",
    "hono": "4.6.0",
    "zod": "3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20241127.0",
    "@vitest/coverage-v8": "2.1.0",
    "typescript": "5.7.0",
    "vitest": "2.1.0",
    "wrangler": "3.93.0"
  }
}
